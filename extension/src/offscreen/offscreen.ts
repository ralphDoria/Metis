// Offscreen document is reserved for a future DRM fallback path (canvas-redraw
// capture for Widevine-protected sources). Reels typically does not require
// it. Kept as a build target so the offscreen API is wired and ready.

export {}
